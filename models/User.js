class User {
  var username;
  var email;
  var group;

  constructor(username, password, email, lastLogin, group) {
    this.username = username;
    this.password = password;
    this.email = email;
    this.lastLogin = lastLogin;
    this.group = group;
  }

  function setGroup(newGroup) {

  }
}
